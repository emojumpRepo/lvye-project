{
  "name": "lvye-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy:frontend": "node script/windows/deploy-frontend.mjs",
    "deploy:backend": "node script/windows/deploy-backend.mjs",
    "deploy:all": "npm run deploy:backend && npm run deploy:frontend",
    "build:backend": "mvn clean package -DskipTests",
    "build:frontend": "cd yudao-ui/lvye-project-frontend && pnpm build:admin && pnpm build:web",
    "build:all": "npm run build:backend && npm run build:frontend",
    "release": "node script/release.mjs",
    "release:deploy": "node script/release-with-deploy.mjs",
    "test:dify": "node script/test-dify.mjs"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "chalk": "^5.6.2",
    "readline-sync": "^1.4.10",
    "ssh2": "^1.17.0"
  }
}
